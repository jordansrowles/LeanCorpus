<!-- filename: `version - YYYY-mm-dd` -->

### Added
- An opt-in CI workflow now repeats the test suite on Linux and Windows to help detect flaky tests (04e066d50)

### Fixed
- HNSW indexes now remain readable when merging removes every node from an upper graph level (04e066d50)